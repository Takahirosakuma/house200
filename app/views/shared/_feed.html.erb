<% if @feed_items %>
  <%= will_paginate @feed_items,
          params: { controller: :static_pages, action: :home } , :previous_label => ' &lt 前へ', :next_label => '次へ &gt'%>
  <ol class="posts">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items,
          params: { controller: :static_pages, action: :home } , :previous_label => ' &lt 前へ', :next_label => '次へ &gt'%>
<% end %>
